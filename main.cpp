// #include "includes/utils/RandomAccessIterator.hpp"
// #include "includes/list.hpp"
// #include "includes/vector.hpp"
// #include <list>
// #include <vector>
#include <iostream>
// #include <iterator>

// int	main() {
// 	ft::list<int> ft_list;
// 	std::list<int> std_list;

// 	std::cout << ft_list.size() << std::endl;
// 	std::cout << std_list.size() << std::endl;
// }

int main() {
	for (int i = 0; i < 1000; i++) {
		std::cout << "auti ";
		std::cout << "AUTI ";
	}
	return (0);
	// ft::vector<int>		own1(5, 100);
	// ft::vector<int>		own2;
	// std::vector<int>	real1(5, 100);
	// std::vector<int>	real2;

	// ft::vector<int>::iterator own_it = own1.begin();
	// ft::vector<int>::iterator own_ite = own1.begin() + 3;
	// std::vector<int>::iterator real_it = real1.begin();
	// std::vector<int>::iterator real_ite = real1.begin() + 3;

	// own2.assign(own_it, own_ite);
	// real2.assign(real_it, real_ite);

	// std::cout << "capacity: " << own1.capacity() << " - " << real1.capacity() << std::endl;
	// std::cout << "size: " << own2.size() << " - " << real2.size() << std::endl;
	// std::cout << "0: " << own2[0] << " - " << real2[0] << std::endl;
	// std::cout << "1: " << own2[1] << " - " << real2[1] << std::endl;
	// std::cout << "2: " << own2[2] << " - " << real2[2] << std::endl;
	// std::cout << "3: " << own2[3] << " - " << real2[3] << std::endl;
}